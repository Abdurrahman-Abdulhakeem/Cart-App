# from rest_framework import serializers

# def validate_password(query):
#     if query['password'] != query['password2']:
#         raise serializers.ValidationError({"Password": "Password fields didn't match."})
#     return query

